{
  "name": "JetClusteringDevContainer",
  "image": "Dockerfile",
  "extensions": [
    // Add any Visual Studio Code extensions you need
  ],
  "runArgs": [
    "--cap-add=SYS_PTRACE",   // Required for debugging in some scenarios
    "--security-opt", "seccomp=unconfined"
  ],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "postCreateCommand": "bash .devcontainer/init.sh"
}
